# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Database (Leave empty for in-memory repository during local development)
DATABASE_TYPE=memory
DATABASE_URL=

# Authentication (For future secured endpoints)
JWT_SECRET=

# LLM Provider (To be configured in Phase 3)
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=

# Speech-to-Text Provider (To be configured in Phase 5)
STT_PROVIDER=
STT_API_KEY=
